# CLI for agents

**AI 에이전트를 위한 CLI는 어떤 모습이어야 할까?**

CLI는 에이전트가 도구를 실행하는 익숙한 방법이다. 명령을 탐색하고, 입력을 전달하고, 출력을 다른 프로그램에 연결할 수 있다.

하지만 명령을 실행할 수 있다는 것만으로 사용에 필요한 정보가 모두 갖춰지는 것은 아니다. 도움말에는 문법이 있지만 작업 순서는 없을 수 있고, 결과를 받아도 요청이 완료됐는지 알기 어려울 수 있다. 여러 명령을 연결해 처리할 수 있는 작업을 에이전트가 매번 나눠 호출하기도 한다.

이 가이드는 **발견에서 실행, 결과 해석과 다음 행동까지 이어지는 CLI의 설계**를 다룬다. 에이전트가 도구를 사용하면서 찾아다니거나 추측해야 했던 정보를 CLI에서 제공하는 방법을 설명한다.

**[가이드 읽기 →](guide/ko/index.md)** · [English](README.md)

### 발견과 사용 지식

도구 이름에서 시작해 필요한 명령과 사용 방법을 찾는다. 도움말은 문법을, 스킬은 작업 흐름을 설명한다. 기능이 많아져도 필요한 부분부터 읽을 수 있도록 구성한다.

### 입력과 실행

흔한 작업을 간단하게 만드는 기본값, 수정할 근거를 제공하는 입력 오류, 파일·stdin을 통한 복잡한 입력을 다룬다. 새로운 판단이 필요 없는 단계는 파이프나 명령 조합으로 한 번에 실행할 수 있게 한다.

### 결과와 다음 행동

JSON과 사람용 표현, 출력량과 페이지 조회, 부분 실패와 비동기 작업의 상태를 설명한다. 결과에 관련된 후속 명령을 제공하되, 조회한 외부 콘텐츠와 도구가 작성한 안내를 구분한다.

### 제작과 검증

명령 선언에서 도움말과 검증을 생성하고, 기능 가까이에 둔 사용 지식을 작업별 가이드로 조립한다. 결정 로직과 부수효과를 나누고, 함수부터 실제 CLI 호출까지 검증하는 구조를 살펴본다.

## 적용 범위

언어와 프레임워크에 독립적인 가이드다. 특정 패키지를 설치하지 않아도 원칙을 적용할 수 있다.

홈 디렉터리·네트워크·인증·장시간 작업은 해당 기능이 필요한 도구의 패턴으로 다룬다. 문서의 명령과 응답은 설계 예시이며, 정확한 명령 이름이나 필드명을 공통 규격으로 정하지 않는다.

영문이 기준이며 한국어판은 같은 내용을 다룬다.

## 참조 구현

| 저장소 | 역할 |
| --- | --- |
| [cli-for-agents-ts](https://github.com/zeakd/cli-for-agents-ts) | TypeScript 패키지 `cli-for-agents`와 실행 가능한 예제 |
| `cli-for-agents-go` — 예정 | Go 구현 |

TypeScript 구현에서 일부 원칙을 코드로 살펴볼 수 있다. 현재 동작은 JSON 기본값과 오류 채널 등에서 가이드의 권고와 다르며, 아직 구현하지 않은 계약도 있다. [버전별 지원 범위](guide/ko/08-evidence-and-scope.md#참조-구현의-지원-범위)에 차이를 정리했다.

라이선스: [MIT](LICENSE)
